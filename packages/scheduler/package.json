{
  "name": "@peer-watchalong/scheduler",
  "version": "0.1.0",
  "private": true,
  "description": "Chunk scheduling and playback synchronization for Peer Watchalong viewer peers.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
